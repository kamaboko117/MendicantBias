import connected from './connected';
import connecting from './connecting';
import disconnected from './disconnected';
import err from './err';

export default { connected, connecting, disconnected, err };